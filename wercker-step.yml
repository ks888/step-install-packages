name: install-packages
version: 1.0.1
description: install-packages step
keywords:
  - apt-get
  - update
  - install
  - packages
properties:
  clear-cache-on-retry:
    type: string
    required: false
    default: "true"
